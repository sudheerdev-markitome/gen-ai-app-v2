services:
  backend:
    build: ./gen-ai-backend
    restart: unless-stopped
    env_file:
      - ./gen-ai-backend/.env
    ports:
      - "8000:8000"
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"